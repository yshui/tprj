Usage:
	nda:
		Automounter, require udisks
	ulm:
		List devices mounted by current user, or all mounted devices if the program is run as root
	uld [-d] <device-path> :
		Unmount specified device, and, if -d is specified, detach it.
